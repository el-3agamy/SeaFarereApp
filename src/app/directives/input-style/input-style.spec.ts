import { InputStyle } from '../input-style';

describe('InputStyle', () => {
  it('should create an instance', () => {
    const directive = new InputStyle();
    expect(directive).toBeTruthy();
  });
});
